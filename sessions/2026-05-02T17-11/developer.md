# 💻 Developer — writer가 제공한 '샘플 사건 파일' 데이터를 입력받아, 핵심 추론 엔진의 최소 기능 제품(MVP)을 구현합니다. 이 MVP는 다음 기능을 포함해야 합니다: 1. 데이터 파싱 및 노드/엣지 생성 기능. 2. 사전에 정의된 규칙 세트(예: 날짜 불일치, 자금 규모 비례 오류)를 기반으로 '모순점 플래그'를 발생시키는 로직 구현. 3. 모순점을 플레이어가 클릭하여 상세한 설명('논리적 증거물')을 얻는 인터페이스의 백엔드 구조 설계.

💻 Developer: 작업 시작합니다.

## 🛠️ Core Inference Engine MVP 구현 (Python)

CEO님의 지시에 따라, 제공된 사건 파일 블루프린트를 기반으로 핵심 추론 엔진의 최소 기능 제품(MVP)을 설계했습니다. 이 코드는 데이터 파싱, 규칙 기반 모순점 플래깅, 그리고 상세 증거물 로직까지 포함합니다.

**사용 방법:**
1.  위 코드를 `inference_engine.py` 파일로 저장합니다.
2.  터미널에서 `python inference_engine.py`를 실행하여 테스트 케이스 1번을 확인합니다.

```python
import
